spring:
  application:
    name: order-service

  kafka:
    bootstrap-servers: kafka-with-kraft.kafka.svc.cluster.local:9092
    consumer:
      group-id: order-service-group
      auto-offset-reset: earliest
      
  datasource:
    url: jdbc:postgresql://postgresql-primary.postgresql.svc.cluster.local:5432/mederma
    username: dexter
    password: "u2RSfgtjMS1cwfE5"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        formate_sql: true
        
logging:
  level:
    org.apache.kafka: DEBUG
